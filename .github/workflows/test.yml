on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.QINIU_SECRET.secret_key }}
        run: |
          echo "ENV: $SUPER_SECRET"
  
  
