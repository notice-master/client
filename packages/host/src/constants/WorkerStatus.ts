export enum WorkerStatus {
  'uninitialized' = 'uninitialized',
  'initialized' = 'initialized',
  'pending' = 'pending',
  'running' = 'running',
  'paused' = 'paused',
}

export enum TaskStatus {
  'pending' = 'pending',
  'successed' = 'successed',
  'failed' = 'failed',
}
